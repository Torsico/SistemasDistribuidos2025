<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Usuarios</title>
	<!-- <link rel="stylesheet" href="stylesheet.css"> -->
</head>
<body>
	
	<%= render "shared/navbar" %>
	
	<%= render "shared/notif" %>
	
	<% unless @badlist %>
	
	<div class="centermini" style="display: flex;">
		<table style="width: 100%; display: table; clear: both;">
		<tr>
			<th></th>
			<th>Usuario</th>
			<th>Nombre</th>
			<th>Apellido</th>
			<th>E-mail</th>
			<th>Rol</th> <%# transformar esto a string despues %>
			<th>Telefono</th>
			<th>Activo</th> <%# transformar a bool o algo %>
		</tr>
		
		<% @usuarios.each do |user| %>
		<tr>
			<td>
				<a href="/usuarios/mod/<%=user.idusuario%>" class="btn"
					title="Modificar este usuario">
    				📋
				</a>
			</td>
			<td><%= user.nombreUsuario %></td>
			<td><%= user.nombre %></td>
			<td><%= user.apellido %></td>
			<td><%= user.email %></td>
			<td><%= $rolelut[user.rol] %></td>
			<td><%= user.telefono %></td>
			<td><%= user.activo %></td>
		</tr>
		<% end %>
		</table>
	</div>
	<div class="centermini" style="display: flex;">
		<a href="/usuarios/alta" class="btn">
			Crear nuevo usuario
    	</a>
	</div>
	
	<% end %>
	
</body>
</html>